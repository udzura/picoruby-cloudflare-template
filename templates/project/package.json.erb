{
  "name": "<%= @name %>",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bundle exec rake build",
    "generate:bindings": "bundle exec rake build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "jsonc-parser": "3.2.0",
    "wrangler": "4.125.0"
  }
}
